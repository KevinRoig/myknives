{% extends 'base.html.twig' %}

{% block title %}Knife{% endblock %}

{% block body %}
    <h1>Knife</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ knife.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ knife.name }}</td>
            </tr>
            <tr>
                <th>Maker</th>
                <td>{{ knife.maker }}</td>
            </tr>
            <tr>
                <th>Blade_length</th>
                <td>{{ knife.bladeLength }}</td>
            </tr>
            <tr>
                <th>Blade_thickness</th>
                <td>{{ knife.bladeThickness }}</td>
            </tr>
            <tr>
                <th>Blade_material</th>
                <td>{{ knife.bladeMaterial }}</td>
            </tr>
            <tr>
                <th>Total_length</th>
                <td>{{ knife.totalLength }}</td>
            </tr>
            <tr>
                <th>Handle_length</th>
                <td>{{ knife.handleLength }}</td>
            </tr>
            <tr>
                <th>Handle_material</th>
                <td>{{ knife.handleMaterial }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ knife.state }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('knife_index') }}">back to list</a>

    <a href="{{ path('knife_edit', {'id': knife.id}) }}">edit</a>

    {{ include('knife/_delete_form.html.twig') }}
{% endblock %}
